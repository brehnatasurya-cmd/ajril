<!DOCTYPE html>
<html lang="id" class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ular Tangga Quiz Aljabar</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="/_sdk/element_sdk.js"></script>
  <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@600;700;800&family=Poppins:wght@500;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }

    html, body, #app {
      height: 100%;
      margin: 0;
      padding: 0;
    }

    .board-grid {
      display: grid;
      grid-template-columns: repeat(10, 1fr);
      gap: 2px;
      background: #ddd;
      padding: 10px;
      border-radius: 12px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.2);
    }

    .box {
      aspect-ratio: 1;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      color: white;
      font-size: 0.9rem;
      position: relative;
      min-height: 50px;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
    }

    .box:hover {
      transform: scale(1.05);
      box-shadow: 0 6px 20px rgba(102, 126, 234, 0.5);
    }

    .box.special-level1 {
      background: linear-gradient(135deg, #84fab0 0%, #8fd3f4 100%);
    }

    .box.special-level2 {
      background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
    }

    .box.special-level3 {
      background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
    }

    .box.has-question::after {
      content: '?';
      position: absolute;
      top: 2px;
      right: 4px;
      font-size: 0.8rem;
      opacity: 0.7;
    }

    .ladder-icon {
      position: absolute;
      font-size: 1.5rem;
      opacity: 0.3;
    }

    .snake-icon {
      position: absolute;
      font-size: 1.5rem;
      opacity: 0.3;
    }

    .player-token {
      position: absolute;
      width: 28px;
      height: 28px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.2rem;
      font-weight: bold;
      border: 3px solid white;
      animation: bounce 0.6s ease-in-out;
      z-index: 10;
    }

    @keyframes bounce {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.2); }
    }

    .player-token.player {
      background: #FF6B6B;
    }

    .player-token.bot {
      background: #4ECDC4;
    }

    @keyframes slideDown {
      0% { transform: translateY(-20px); opacity: 0; }
      100% { transform: translateY(0); opacity: 1; }
    }

    .question-popup {
      animation: slideDown 0.4s ease-out;
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.05); }
    }

    .dice-roll {
      animation: pulse 0.6s ease-in-out;
    }

    .correct-answer {
      background: #2ecc71 !important;
      animation: scaleIn 0.4s ease-out;
    }

    .wrong-answer {
      background: #e74c3c !important;
      animation: scaleIn 0.4s ease-out;
    }

    @keyframes scaleIn {
      0% { transform: scale(0.5); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }

    @keyframes moveToken {
      0% { opacity: 0; transform: scale(0.5); }
      50% { opacity: 1; transform: scale(1.1); }
      100% { opacity: 1; transform: scale(1); }
    }

    .token-move {
      animation: moveToken 0.8s ease-out;
    }

    @keyframes fadeOut {
      0% {
        opacity: 1;
        transform: scale(1);
      }
      100% {
        opacity: 0;
        transform: scale(0.95);
      }
    }

    @keyframes fadeIn {
      0% {
        opacity: 0;
        transform: scale(0.95);
      }
      100% {
        opacity: 1;
        transform: scale(1);
      }
    }

    .question-fade-out {
      animation: fadeOut 0.5s ease-out forwards;
    }

    .question-fade-in {
      animation: fadeIn 0.6s ease-out;
    }

    @keyframes slideOutUp {
      0% {
        opacity: 1;
        transform: translateY(0);
      }
      100% {
        opacity: 0;
        transform: translateY(-30px);
      }
    }

    @keyframes slideInDown {
      0% {
        opacity: 0;
        transform: translateY(-30px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .answer-feedback-out {
      animation: slideOutUp 0.4s ease-out forwards;
    }

    .answer-feedback-in {
      animation: slideInDown 0.5s ease-out 0.2s both;
    }

    @keyframes pulse-success-bg {
      0%, 100% {
        background: rgba(46, 204, 113, 0.1);
      }
      50% {
        background: rgba(46, 204, 113, 0.3);
      }
    }

    @keyframes pulse-error-bg {
      0%, 100% {
        background: rgba(231, 76, 60, 0.1);
      }
      50% {
        background: rgba(231, 76, 60, 0.3);
      }
    }

    .answer-correct-feedback {
      animation: pulse-success-bg 0.8s ease-in-out;
    }

    .answer-wrong-feedback {
      animation: pulse-error-bg 0.8s ease-in-out;
    }

    .ladder {
      position: absolute;
      width: 3px;
      background: linear-gradient(180deg, #2ecc71, #27ae60);
      opacity: 0.4;
    }

    .snake {
      position: absolute;
      width: 3px;
      background: linear-gradient(180deg, #e74c3c, #c0392b);
      opacity: 0.4;
    }

    .notification {
      animation: slideDown 0.4s ease-out;
      padding: 12px 20px;
      border-radius: 8px;
      font-weight: 600;
      margin-bottom: 8px;
    }

    .notification.success {
      background: #2ecc71;
      color: white;
    }

    .notification.error {
      background: #e74c3c;
      color: white;
    }

    .notification.info {
      background: #3498db;
      color: white;
    }

    .game-over-modal {
      animation: slideDown 0.5s ease-out;
      background: linear-gradient(135deg, rgba(51, 51, 51, 0.98), rgba(30, 30, 30, 0.98));
      backdrop-filter: blur(10px);
    }

    .score-card {
      border-radius: 12px;
      padding: 16px;
      transition: all 0.3s ease;
    }

    .score-card:hover {
      transform: translateY(-4px);
    }

    .turn-indicator {
      animation: pulse 1s infinite;
    }

    .level-badge {
      position: absolute;
      top: 2px;
      left: 2px;
      font-size: 0.7rem;
      background: rgba(0, 0, 0, 0.4);
      padding: 2px 4px;
      border-radius: 4px;
      font-weight: 700;
    }

    .timer-display {
      font-size: 2.5rem;
      font-weight: 900;
      text-align: center;
      padding: 16px;
      border-radius: 12px;
      margin-bottom: 12px;
    }

    .timer-warning {
      background: #fff3cd;
      color: #856404;
      animation: pulse 1s infinite;
    }

    .timer-danger {
      background: #f8d7da;
      color: #721c24;
      animation: pulse 0.5s infinite;
    }

    .timer-normal {
      background: #d1ecf1;
      color: #0c5460;
    }
  </style>
</head>
<body class="h-full bg-gradient-to-br from-blue-100 via-purple-100 to-pink-100 overflow-hidden">
  <div id="app" class="h-full w-full overflow-auto flex flex-col"></div>

  <script>
    const defaultConfig = {
      game_title: "🐍 ULAR TANGGA QUIZ ALJABAR 📐",
      player_name: "Pemain",
      bot_name: "BOT AI"
    };

    let config = { ...defaultConfig };

    // ================== DATA SOAL ==================
    const questions = {
      1: { question: "3x + 2 = 11, nilai x adalah?", type: "short", answer: "3", level: 1 },
      2: { question: "2a + 3a = ?", type: "multiple", options: ["4a", "5a", "6a", "7a"], correct: 1, level: 1 },
      3: { question: "5b - 2b + 3b = ?", type: "multiple", options: ["4b", "5b", "6b", "7b"], correct: 2, level: 1 },
      4: { question: "Sederhanakan: x + x + x", type: "short", answer: "3x", level: 1 },
      5: { question: "2(a + 3) = ?", type: "multiple", options: ["2a + 6", "2a + 3", "a + 6", "2a"], correct: 0, level: 1 },
      6: { question: "3x - 5 + 2x + 8 = ?", type: "multiple", options: ["5x + 3", "6x + 3", "x + 3", "5x - 3"], correct: 0, level: 1 },
      7: { question: "Koefisien x dalam 7x + 5y adalah?", type: "short", answer: "7", level: 1 },
      8: { question: "4(2m) = ?", type: "short", answer: "8m", level: 1 },
      9: { question: "-3a + 5a = ?", type: "multiple", options: ["a", "2a", "-2a", "8a"], correct: 1, level: 1 },
      10: { question: "6x - 6x + 2 = ?", type: "short", answer: "2", level: 1 },
      15: { question: "x + 5 = 12, x = ?", type: "short", answer: "7", level: 1 },
      20: { question: "2x - 3 = 7, x = ?", type: "multiple", options: ["4", "5", "6", "3"], correct: 1, level: 2 },
      25: { question: "3x + 2x - x = ?", type: "short", answer: "4x", level: 2 },
      30: { question: "5(x - 2) = 10, x = ?", type: "multiple", options: ["3", "4", "5", "6"], correct: 2, level: 2 },
      35: { question: "2x + 7 = 3x - 5, x = ?", type: "short", answer: "12", level: 2 },
      40: { question: "4x - 8 = 2x + 4, x = ?", type: "multiple", options: ["6", "8", "10", "12"], correct: 0, level: 2 },
      45: { question: "3(2x - 1) = 15, x = ?", type: "short", answer: "3", level: 2 },
      50: { question: "x/2 + 3 = 5, x = ?", type: "multiple", options: ["2", "4", "6", "8"], correct: 1, level: 2 },
      55: { question: "2(x + 3) - 4 = 10, x = ?", type: "short", answer: "4", level: 2 },
      60: { question: "5x - 2 = 3x + 8, x = ?", type: "multiple", options: ["3", "4", "5", "6"], correct: 2, level: 2 },
      65: { question: "x² - 5x + 6 = 0, faktornya adalah?", type: "multiple", options: ["(x-2)(x-3)", "(x+2)(x+3)", "(x-1)(x-6)", "(x-2)(x+3)"], correct: 0, level: 3 },
      70: { question: "2x² + 3x - 2 = 0, x = ?", type: "short", answer: "0.5 atau -2", level: 3 },
      75: { question: "Jika x² - 7x + 12 = 0, nilai x adalah?", type: "multiple", options: ["3 atau 4", "2 atau 5", "1 atau 6", "3 atau 5"], correct: 0, level: 3 },
      80: { question: "Keliling = 2x + 8, jika K = 20, x = ?", type: "short", answer: "6", level: 3 },
      85: { question: "(2x - 3)² = ?", type: "multiple", options: ["4x² - 12x + 9", "4x² - 6x + 9", "4x² - 12x + 6", "2x² - 12x + 9"], correct: 0, level: 3 },
      90: { question: "Luas persegi = s², jika L = 25, s = ?", type: "short", answer: "5", level: 3 },
      95: { question: "3x - 2y = 8 dan x + y = 4, x = ?", type: "multiple", options: ["2", "3", "4", "5"], correct: 2, level: 3 },
      100: { question: "x³ - 8 = (x - 2)(x² + 2x + ?)", type: "short", answer: "4", level: 3 }
    };

    // Tambah soal di box-box lainnya
    for (let i = 1; i <= 100; i++) {
      if (!questions[i]) {
        const level = i <= 30 ? 1 : i <= 60 ? 2 : 3;
        if (Math.random() > 0
